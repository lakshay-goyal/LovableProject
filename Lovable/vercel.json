{
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps && npm rebuild lightningcss --platform=linux --arch=x64",
  "build": {
    "env": {
      "NODE_OPTIONS": "--max-old-space-size=4096"
    }
  }
}
